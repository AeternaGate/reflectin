# Reflectin: Этапы 0+1 (фундамент + генерация контента) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Работающий Telegram-бот с Mini App: webhook, авторизация initData, генерация постов через OpenRouter (fallback-цепочка), черновики, квоты 5/день, стартовый экран Mini App по согласованному дизайну v4.

**Architecture:** Supabase (Postgres + Edge Functions на Deno). Вход боту — webhook (`secret_token` в заголовке), авторизация Mini App — initData (HMAC-SHA256, ключ `"WebAppData"`, секрет `BOT_TOKEN`, TTL 24ч, constant-time). БД — через service role, api = доверенная граница. AI — OpenRouter REST через `fetch`, ростер моделей в env с fallback по цепочке. Бот и api-функция делят чистые модули `_shared/*`, каждый имеет `deno test`.

**Tech Stack:** Deno 2 (Edge Functions + `deno test`), grammY не используется напрямую — общаемся с Bot API через `fetch` (один endpoивт webhook, grammY не нужен, YAGNI), Postgres (Supabase), OpenRouter (REST), frontend Vite + React 19 + TS + Tailwind v4 + `@phosphor-icons/react`.

## Global Constraints

- **Русский язык интерфейса и голоса бота.** Без эмодзи в контенте и UI. Без градиентов.
- **Дизайн v4:** фон монохромный (`#0b0b0d` / панели `#151518` / рамки `#222227`, БЕЗ зелёного подтона), акцент `#b4ff5a`, логотип — символ `#`, квота — сегменты, «лесенка» табов на старте, карточки с левой цветной полосой по рубрике.
- **Рубрики и веса:** польза 40, вовлечение 30, продажи 20, личное 10 (сумма = 100).
- **Модели: без хардкода ротера в функциях** — env `OPENROUTER_MODELS`, дефолт `nvidia/nemotron-3-ultra-550b-a55b:free,inclusionai/ling-3.0-flash:free,openai/gpt-oss-20b:free`.
- **Квота Free = 2 генерации/день** из env `FREE_DAILY_QUOTA` (ТЗ), счётчик — строки `generation_log` за календарную дату (UTC).
- **Никаких эмодзи в коде-контент: проверка в `validatePlan`; голосовой промпт запрещает эмодзи.**
- Версии: Deno ≥ 2.0, Node ≥ 20. Тесты запускаются `deno test` из папки `supabase/`. Frontend проверяется `npm run typecheck` + `npm run build`.

## File Structure

```
reflectin/
  .env.example
  README.md
  supabase/
    deno.json
    functions/
      telegram-bot/index.ts          # webhook вход
      telegram-bot/handler.ts        # чистая логика (тестируется)
      api/index.ts                   # Mini App API, initData-auth
      _shared/cors.ts                # CORS + okJson/errJson
      _shared/json.ts                # безопасный парсинг LLM-вывода
      _shared/initdata.ts            # HMAC-SHA256 валидация initData
      _shared/prompts.ts             # рубрики, валидатор плана, промпты
      _shared/ai.ts                  # ростер + fallback-цепочка OpenRouter
      _shared/db.ts                  # типизированные запросы (createClient)
    migrations/0001_init.sql
    tests/
      fake_db.ts                     # in-memory мок supabase-клиента
      fake_initdata.ts               # подписывает initData для тестов
      json_test.ts  initdata_test.ts  prompts_test.ts  ai_test.ts
      db_test.ts  migration_test.ts  tgbot_test.ts  api_test.ts
  scripts/
    deploy.ps1  set-webhook.ps1  set-menu-button.ps1
  webapp/
    package.json  vite.config.ts  tsconfig.json  index.html
    src/main.tsx  src/index.css  src/lib/api.ts  src/App.tsx
  docs/superpowers/specs/2026-08-20-reflectin-design.md
```

## Межзадачные интерфейсы (типы-контракты)

> Исполнитель задачи видит только свою задачу; ниже контракты соседей.

- `json.ts`: `safeJsonParse(text, fallback=null): unknown`; `extractJson(text): unknown`; `isJsonObject(v): v is Record<string, unknown>`.
- `initdata.ts`: `parseInitData(raw): Record<string,string>`; `buildDataCheckString(fields): string` (без `hash`, сортировка по ключу, `\n`); `deriveSecretKey(botToken): Promise<Uint8Array>`; `computeHash(dcs, secretKey): Promise<string>` (hex); `safeEqual(a,b): boolean`; `validateInitData(raw, botToken, maxAgeSeconds=86400, nowSeconds?): Promise<boolean>`; `initDataUser(raw): {id:number; first_name:string} | null`.
- `prompts.ts`: `RUBRICS` (as const 4 шт.), `RUBRIC_WEIGHTS: Record<Rubric,number>`; `RUBRIC_CAPTION`; `validatePlan(items: unknown): {ok:true; plan:PlanItem[]}|{ok:false; reason:string}`; `buildPostPrompt({topic, rubric, channelTitle?}): string`.
- `ai.ts`: `DEFAULT_MODELS: string[]`; `resolveModels(envModels?): string[]`; `complete({apiKey, model, messages, temperature?, maxTokens?, fetchImpl?}): Promise<string>`; `completeWithFallback({apiKey, models, messages, temperature?, fetchImpl?}): Promise<{text: string; model: string}>`; `ChatMessage = {role:'system'|'user'|'assistant'; content:string}`.
- `db.ts`: `interface Db { from(table): DbTable }`; `dbFromEnv(createClient?)`; `quotaUsedToday(db, userId, dateISO): Promise<number>`; `getOrCreateUser(db, telegramId, firstName): Promise<{id:string; plan?:string}>`; `insertPost(db, {user_id, content, rubric?, status}): Promise<string>`; `logGeneration(db, {user_id, feature, model})`; `getDrafts(db, userId): Promise<{id; content; rubric; created_at}[]>`.
- `cors.ts`: `corsHeaders(): Record<string,string>`; `okJson(body): Response`; `errJson(status, message): Response`.
- Общий инъекционный контракт генерации: `generate: (prompt: string) => Promise<{ text: string; model: string }>` (в тестах — фейк, в проде — `completeWithFallback`).
- `telegram-bot/handler.ts`: `handleUpdate(update: unknown, deps: BotDeps): Promise<string | null>`; `BotDeps = { db, freeQuota, webappUrl, generate, sendMessage(chatId, text, opts?) }`.
- `api/handler.ts`: `handleApiRequest(req, deps: ApiDeps): Promise<Response>`; `ApiDeps = { botToken, db, apiKey, models, freeQuota, generate, nowSeconds? }`. `api/index.ts` — только `Deno.serve` + сборка deps из env (тесты импортируют `handler.ts`, чтобы не заводить сервер).

---

## Task 1: Скелет проекта (структура, git, env, README)

Уже созданы в текущиессии: структура каталогов, `reflectin/.env.example`, `reflectin/README.md`, `supabase/deno.json`. Задача: проверить, инициализировать git и снять контрольную точку.

**Files:**
- Verify: `reflectin/.env.example`, `reflectin/README.md`, `reflectin/supabase/deno.json`
- Create: none

- [ ] **Step 1: Проверить структуру**
Run: `tree /F reflectin` (Windows) — ожидаются `supabase\deno.json`, `.env.example` на корне.
- [ ] **Step 2: Git init**
```bash
cd D:\Bots\reflectin
git init -q
git branch -m main
```
- [ ] **Step 3: Коммит**

```bash
git add .env.example README.md supabase/deno.json
git commit -m "chore: initial scaffold"
```
Ожидание: commit создан (`git log --oneline`).

---

## Task 2: `_shared/json.ts` — безопасный JSON для LLM-вывода

**Files:**
- Create: `supabase/functions/_shared/json.ts`
- Test: `supabase/tests/json_test.ts` (уже написан)

- [ ] **Step 1: Убедиться, что тест падает**

Run: `deno test tests/json_test.ts --no-check`
Expected: FAIL — `Module not found "…/functions/_shared/json.ts"`.

- [ ] **Step 2: Реализация**

```ts
export function safeJsonParse(text: string, fallback: unknown = null): unknown {
  if (!text) return fallback;
  try {
    return JSON.parse(text);
  } catch {
    return fallback;
  }
}

export function isJsonObject(v: unknown): v is Record<string, unknown> {
  return typeof v === "object" && v !== null && !Array.isArray(v);
}

/** Вытаскивает первый JSON-объект или массив из текста LLM (в т.ч. из ```json-фенсов). */
export function extractJson(text: string): unknown {
  const obj = text.match(/\{[\s\S]*\}/);
  if (obj) {
    try {
      return JSON.parse(obj[0]);
    } catch {
      /* падаем на массив */
    }
  }
  const arr = text.match(/\[[\s\S]*\]/);
  if (arr) {
    try {
      return JSON.parse(arr[0]);
    } catch {
      return null;
    }
  }
  return null;
}
```

- [ ] **Step 3: Проверить зелёный**

Run: `deno test tests/json_test.ts`
Expected: 8 passed.

- [ ] **Step 4: Коммит**

```bash
git add supabase/functions/_shared/json.ts supabase/tests/json_test.ts
git commit -m "feat: безопасный JSON-парсинг для LLM-вывода"
```

---

## Task 3: `_shared/initdata.ts` — валидация initData (HMAC-SHA256)

**Files:**
- Create: `supabase/functions/_shared/initdata.ts`
- Test: `supabase/tests/initdata_test.ts` (написан) + helper `supabase/tests/fake_initdata.ts` (создан)

- [ ] **Step 1: Тест красный**
Run: `deno test tests/initdata_test.ts --no-check`
Expected: `Module not found "…/initdata.ts"`.

- [ ] **Step 2: Реализация**

```ts
import { safeJsonParse } from "./json.ts";

export function parseInitData(raw: string): Record<string, string> {
  const out: Record<string, string> = {};
  for (const pair of raw.split("&")) {
    const eq = pair.indexOf("=");
    if (eq <= 0) continue;
    out[decodeURIComponent(pair.slice(0, eq))] = decodeURIComponent(pair.slice(eq + 1));
  }
  return out;
}

export function buildDataCheckString(fields: Record<string, string>): string {
  return Object.keys(fields)
    .filter((k) => k !== "hash")
    .sort()
    .map((k) => `${k}=${fields[k]}`)
    .join("\n");
}

export async function deriveSecretKey(botToken: string): Promise<Uint8Array> {
  const key = await crypto.subtle.importKey(
    "raw",
    new TextEncoder().encode("WebAppData"),
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign("HMAC", key, new TextEncoder().encode(botToken));
  return new Uint8Array(sig);
}

export async function computeHash(dataCheckString: string, secretKey: Uint8Array): Promise<string> {
  const key = await crypto.subtle.importKey(
    "raw",
    secretKey,
    { name: "HMAC", hash: "SHA-256" },
    false,
    ["sign"],
  );
  const sig = await crypto.subtle.sign("HMAC", key, new TextEncoder().encode(dataCheckString));
  return [...new Uint8Array(sig)].map((b) => b.toString(16).padStart(2, "0")).join("");
}

export function safeEqual(a: string, b: string): boolean {
  if (a.length !== b.length) return false;
  let d = 0;
  for (let i = 0; i < a.length; i++) d |= a.charCodeAt(i) ^ b.charCodeAt(i);
  return d === 0;
}

export async function validateInitData(
  raw: string,
  botToken: string,
  maxAgeSeconds = 86400,
  nowSeconds = Math.floor(Date.now() / 1000),
): Promise<boolean> {
  const fields = parseInitData(raw);
  const hash = fields["hash"];
  const authDate = Number(fields["auth_date"]);
  if (!hash || !Number.isFinite(authDate) || nowSeconds - authDate > maxAgeSeconds) return false;
  const secret = await deriveSecretKey(botToken);
  const expected = await computeHash(buildDataCheckString(fields), secret);
  return safeEqual(expected, hash);
}

export function initDataUser(raw: string): { id: number; first_name: string } | null {
  const u = safeJsonParse(parseInitData(raw)["user"] ?? "", null);
  if (!isUserPayload(u)) return null;
  return { id: Number(u.id), first_name: String(u.first_name ?? "") };
}

function isUserPayload(v: unknown): v is { id: number | string; first_name?: unknown } {
  return typeof v === "object" && v !== null && "id" in v;
}
```

- [ ] **Step 3: Проверить зелёный**
Run: `deno test tests/initdata_test.ts`
Expected: все тесты проходят (самоподписанный вектор + тамперинг + TTL + чужой токен).

- [ ] **Step 4: Коммит**
```bash
git add supabase/functions/_shared/initdata.ts supabase/tests/initdata_test.ts supabase/tests/fake_initdata.ts
git commit -m "feat: initData HMAC-SHA256 валидация"
```

---

## Task 4: `_shared/prompts.ts` — рубрики, валидатор плана, промпты

**Files:**
- Create: `supabase/functions/_shared/prompts.ts`
- Test: `supabase/tests/prompts_test.ts` (написан)

- [ ] **Step 1: Тест красный**
Run: `deno test tests/prompts_test.ts --no-check`

- [ ] **Step 2: Реализация**

```ts
export const RUBRICS = ["польза", "вовлечение", "продажи", "личное"] as const;
export type Rubric = (typeof RUBRICS)[number];

export const RUBRIC_WEIGHTS: Record<Rubric, number> = {
  польза: 40,
  вовлечение: 30,
  продажи: 20,
  личное: 10,
};

export const RUBRIC_CAPTION: Record<Rubric, string> = {
  польза: "дай конкретную пользу: инструкция, лайфхак, шаблон",
  вовлечение: "вовлекай: вопрос, опрос, история с интригой",
  продажи: "мягкая продажа: покажи результат и призыв к действию",
  личное: "личное из жизни автора: эмоция и честность",
};

export interface PlanItem { day: number; rubric: Rubric; topic: string }
export type PlanResult =
  | { ok: true; plan: PlanItem[] }
  | { ok: false; reason: string };

const VALID_DAYS = [1, 2, 3, 4, 5, 6, 7];

export function validatePlan(items: unknown): PlanResult {
  if (!Array.isArray(items) || items.length === 0) {
    return { ok: false, reason: "план пуст или не массив" };
  }
  const seen = new Set<number>();
  for (const it of items) {
    if (it === null || typeof it !== "object") return { ok: false, reason: "элемент не объект" };
    const item = it as Record<string, unknown>;
    const day = Number(item.day);
    if (!VALID_DAYS.includes(day)) return { ok: false, reason: `неверный день: ${item.day}` };
    if (seen.has(day)) return { ok: false, reason: `день повторяется: ${item.day}` };
    seen.add(day);
    if (!RUBRICS.includes(item.rubric as Rubric)) return { ok: false, reason: `неверная рубрика: ${item.rubric}` };
    if (typeof item.topic !== "string" || item.topic.trim().length === 0) {
      return { ok: false, reason: `пустая тема в день ${item.day}` };
    }
  }
  return { ok: true, plan: items as PlanItem[] };
}

export function buildPostPrompt(opts: { topic: string; rubric: Rubric; channelTitle?: string }): string {
  return [
    `Ты — редактор телеграм-канала «${opts.channelTitle ?? "Без названия"}».`,
    `Напиши пост в рубрике «${opts.rubric}»: ${RUBRIC_CAPTION[opts.rubric]}.`,
    "Правила: без эмодзи, короткие абзацы, заголовок-крючок первым делом, живой голос автора.",
    `Тема: ${opts.topic}`,
    "Верни ТОЛЬКО текст поста без комментариев.",
  ].join("\n");
}
```

- [ ] **Step 3: Проверить зелёный**
Run: `deno test tests/prompts_test.ts`

- [ ] **Step 4: Коммит**
```bash
git add supabase/functions/_shared/prompts.ts supabase/tests/prompts_test.ts
git commit -m "feat: рубрики 40/30/20/10, валидатор контент-плана, промпты"
```

---

## Task 5: `_shared/ai.ts` — OpenRouter с fallback-цепочкой

**Files:**
- Create: `supabase/functions/_shared/ai.ts`
- Test: `supabase/tests/ai_test.ts` (написан, fetch инжектится)

- [ ] **Step 1: Тест красный**
Run: `deno test tests/ai_test.ts --no-check`

- [ ] **Step 2: Реализация**

```ts
export const DEFAULT_MODELS = [
  "nvidia/nemotron-3-ultra-550b-a55b:free",
  "inclusionai/ling-3.0-flash:free",
  "openai/gpt-oss-20b:free",
  "openrouter/free",
];

export interface ChatMessage { role: "system" | "user" | "assistant"; content: string }

export function resolveModels(envModels?: string): string[] {
  const raw = envModels?.trim();
  if (!raw) return DEFAULT_MODELS;
  return raw.split(",").map((s) => s.trim()).filter(Boolean);
}

export interface CompleteOptions {
  apiKey: string;
  model: string;
  messages: ChatMessage[];
  temperature?: number;
  maxTokens?: number;
  fetchImpl?: typeof fetch;
}

export async function complete(opts: CompleteOptions): Promise<string> {
  const fetchImpl = opts.fetchImpl ?? fetch;
  const res = await fetchImpl("https://openrouter.ai/api/v1/chat/completions", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${opts.apiKey}`,
    },
    body: JSON.stringify({
      model: opts.model,
      messages: opts.messages,
      temperature: opts.temperature ?? 0.8,
      max_tokens: opts.maxTokens ?? 900,
    }),
  });
  if (!res.ok) throw new Error(`OpenRouter ${res.status}`);
  const body = await res.json();
  const content = body?.choices?.[0]?.message?.content;
  if (typeof content !== "string") throw new Error("OpenRouter: пустой ответ");
  return content;
}

export interface CompletionResult { text: string; model: string }

export async function completeWithFallback(opts: {
  apiKey: string;
  models: string[];
  messages: ChatMessage[];
  temperature?: number;
  fetchImpl?: typeof fetch;
}): Promise<CompletionResult> {
  const errors: string[] = [];
  for (const model of opts.models) {
    try {
      const text = await complete({ ...opts, model });
      return { text, model };
    } catch (e) {
      errors.push(`${model}: ${String(e)}`);
    }
  }
  throw new Error(`Все модели недоступны: ${errors.join("; ")}`);
}
```

- [ ] **Step 3: Проверить зелёный**
Run: `deno test tests/ai_test.ts`
Expected: 8 passed (успех на 1-й, fallback на 2-й, все упали → throw).

- [ ] **Step 4: Коммит**
```bash
git add supabase/functions/_shared/ai.ts supabase/tests/ai_test.ts
git commit -m "feat: OpenRouter fallback-цепочка"
```

---

## Task 6: Миграция `0001_init.sql`

**Files:**
- Create: `supabase/migrations/0001_init.sql`
- Test: `supabase/tests/migration_test.ts` (написан; читает SQL-файл)

- [ ] **Step 1: Тест красный**
Run: `deno test tests/migration_test.ts --no-check`
Expected: FAIL — файл не найден (Deno.readTextFile).

- [ ] **Step 2: SQL**

```sql
create table public.users (
  id uuid primary key default gen_random_uuid(),
  telegram_id bigint not null unique,
  first_name text not null default '',
  plan text not null default 'free',
  created_at timestamptz not null default now()
);

create table public.channels (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.users(id) on delete cascade,
  telegram_channel_id bigint,
  title text not null default '',
  created_at timestamptz not null default now()
);

create table public.posts (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.users(id) on delete cascade,
  channel_id uuid references public.channels(id) on delete set null,
  content text not null,
  rubric text not null default 'польза',
  status text not null default 'draft',
  created_at timestamptz not null default now()
);

create table public.generation_log (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.users(id) on delete cascade,
  feature text not null default 'post',
  model text not null,
  created_at timestamptz not null default now()
);

create index idx_generation_log_user_date
  on public.generation_log (user_id, created_at);
```

- [ ] **Step 3: Зелёный тест**
Run: `deno test tests/migration_test.ts`
Expected: 2 passed (4 таблицы с колонками; индекс).

- [ ] **Step 4: Коммит**
```bash
git add supabase/migrations/0001_init.sql supabase/tests/migration_test.ts
git commit -m "feat: схема начальных 4 таблиц"
```

---

## Task 7: `_shared/db.ts` — типизированные запросы

**Files:**
- Create: `supabase/functions/_shared/db.ts`
- Test: `supabase/tests/db_test.ts` (написан) + `supabase/tests/fake_db.ts` (создан)

- [ ] **Step 1: Тест красный**
Run: `deno test tests/db_test.ts --no-check`

- [ ] **Step 2: Реализация**

```ts
/** Структурный тип: функции принимают любой объект с .from (реальный клиент или мок). */
export interface DbTable {
  select: (cols?: string) => DbTable;
  eq: (k: string, v: unknown) => DbTable;
  maybeSingle: () => Promise<{ data: unknown; error: { message: string } | null }>;
  single: () => Promise<{ data: unknown; error: { message: string } | null }>;
  data: unknown;
  error: { message: string } | null;
  insert: (row: Record<string, unknown>) => DbTable;
}
export interface Db { from: (table: string) => DbTable }

export function dbFromEnv(createClient: (url: string, key: string) => unknown = defaultCreateClient): Db {
  const url = Deno.env.get("SUPABASE_URL");
  const key = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
  if (!url || !key) throw new Error("SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY не заданы");
  return createClient(url, key) as Db;
}

function defaultCreateClient(url: string, key: string): unknown {
  const { createClient } = loadSupabaseJs();
  return createClient(url, key);
}

// ленивый импорт: модуль не тянет supabase-js в тестах, рост только при вызове в функциях
let _supabase: typeof import("npm:@supabase/supabase-js@2");
function loadSupabaseJs() {
  if (!_supabase) _supabase = import("npm:@supabase/supabase-js@2");
  return _supabase;
}

export interface PostRow { id: string; content: string; rubric: string; created_at: string; status: string }

export async function quotaUsedToday(db: Db, userId: string, date: string): Promise<number> {
  const { data, error } = await db
    .from("generation_log")
    .select("created_at")
    .eq("user_id", userId);
  if (error) throw new Error(`quotaUsedToday: ${error.message}`);
  const start = `${date}T00:00:00Z`;
  const end = `${date}T23:59:59.999Z`;
  const rows = (data ?? []) as Array<{ created_at: string }>;
  return rows.filter((r) => r.created_at >= start && r.created_at < end).length;
}

export async function getOrCreateUser(db: Db, telegramId: number, firstName: string): Promise<{ id: string; plan?: string }> {
  const { data: existing, error: err1 } = await db
    .from("users").select("id, plan").eq("telegram_id", telegramId).maybeSingle();
  if (err1) throw new Error(`getOrCreateUser: ${err1.message}`);
  if (existing) return { id: (existing as { id: string }).id, plan: (existing as { plan?: string }).plan ?? "free" };
  const { data: created, error: err2 } = await db
    .from("users")
    .insert({ telegram_id: telegramId, first_name: firstName })
    .select().single();
  if (err2 || !created) throw new Error(`getOrCreateUser insert: ${err2?.message}`);
  return { id: (created as { id: string }).id, plan: (created as { plan?: string }).plan ?? "free" };
}

export async function insertPost(
  db: Db,
  p: { user_id: string; content: string; rubric?: string; status: "draft" | "published" },
): Promise<string> {
  const { data, error } = await db
    .from("posts")
    .insert({ user_id: p.user_id, content: p.content, rubric: p.rubric ?? "польза", status: p.status })
    .select().single();
  if (error || !data) throw new Error(`insertPost: ${error?.message}`);
  return (data as { id: string }).id;
}

export async function logGeneration(
  db: Db,
  p: { user_id: string; feature: string; model: string },
): Promise<void> {
  const { error } = await db.from("generation_log").insert(p);
  if (error) throw new Error(`logGeneration: ${error.message}`);
}

export async function getDrafts(
  db: Db,
  userId: string,
): Promise<Array<{ id: string; content: string; rubric: string; created_at: string }>> {
  const { data, error } = await db.from("posts").select("id, content, rubric, created_at").eq("user_id", userId).eq("status", "draft");
  if (error) throw new Error(`getDrafts: ${error.message}`);
  return (data ?? []) as Array<{ id: string; content: string; rubric: string; created_at: string }>;
}
```

**Важно про `logGeneration` и select**: `db.from("generation_log").insert(p)` возвращает объект с `.data`, `.error` (по чейну). В моке `insert` возвращает `{...base}` c `data`/`error`. Ок.

- [ ] **Step 3: Проверить зелёный**
Run: `deno test tests/db_test.ts`
Expected: 7 passed.

- [ ] **Step 4: Полный прогон_shared**
Run: `deno test`
Expected: json + initdata + prompts + ai + migration + db — все зелёные.

- [ ] **Step 5: Коммит**
```bash
git add supabase/functions/_shared/db.ts supabase/tests/db_test.ts supabase/tests/fake_db.ts
git commit -m "feat: запросы БД (квота, юзер, посты, логи)"
```

---

## Task 8: Кросс-модульный файл `_shared/cors.ts` + webhook-бот

**Files:**
- Create: `supabase/functions/_shared/cors.ts`, `supabase/functions/telegram-bot/index.ts`, `supabase/functions/telegram-bot/handler.ts`
- Test: `supabase/tests/tgbot_test.ts`

- [ ] **Step 1: Написать падающий тест**

`supabase/tests/tgbot_test.ts`:
```ts
import { assertEquals, assert } from "jsr:@std/assert";
import { handleUpdate, type BotDeps } from "../functions/telegram-bot/handler.ts";
import { makeFakeDb } from "./fake_db.ts";

interface Sent { chat: number; text: string; opts?: Record<string, unknown> }

function deps(over: Partial<BotDeps> = {}) {
  const sends: Sent[] = [];
  const gens: string[] = [];
  const d: BotDeps = {
    db: makeFakeDb({}),
    freeQuota: 5,
    webappUrl: "https://app.example.com",
    generate: async (p) => { gens.push(p); return { text: "Сгенерированный пост", model: "m/free" }; },
    sendMessage: async (chat, text, opts) => { sends.push({ chat, text, opts }); return { ok: true }; },
    ...over,
  };
  return { deps: d, sends, gens };
}

const START_UPDATE = {
  message: { message_id: 1, chat: { id: 10 }, from: { id: 123, first_name: "Дима" }, text: "/start" },
};
const GEN_UPDATE = {
  message: { message_id: 2, chat: { id: 10 }, from: { id: 123, first_name: "Дима" }, text: "Напиши про отпуск" },
};

const today = new Date().toISOString().slice(0, 10);

Deno.test("на /start отвечает приветствием с webapp-кнопкой", async () => {
  const { deps: d, sends } = deps();
  await handleUpdate(START_UPDATE, d);
  assert(sends.length === 1);
  assert(sends[0].text.includes("Reflectin"));
  assert(JSON.stringify(sends[0].opts).includes(d.webappUrl));
});

Deno.test("текстовое сообщение — генерация + черновик + лог", async () => {
  const db = makeFakeDb({});
  const { deps: d, sends, gens } = deps({ db });
  const r = await handleUpdate(GEN_UPDATE, d);
  assertEquals(r, "post");
  assert(gens.length === 1);
  assert(gens[0].includes("отпуск"));
  assert(sends[0].text === "Сгенерированный пост");
  assert(db.calls.some((c) => c.startsWith("insert:posts")));
  assert(db.calls.some((c) => c.startsWith("insert:generation_log")));
});

Deno.test("квота исчерпана — не генерируем, отвечаем", async () => {
  const rows = Array.from({ length: 5 }).map((_, i) => ({
    user_id: "u1", feature: "post", model: "m", created_at: `${today}T0${i}:00:00Z`,
  }));
  const db = makeFakeDb({ generation_log: rows, users: [{ id: "u1", telegram_id: 123 }] });
  const { deps: d, sends, gens } = deps({ db });
  const r = await handleUpdate(GEN_UPDATE, d);
  assertEquals(r, "quota");
  assert(gens.length === 0);
  assert(sends[0].text.includes("лимит"));
});

Deno.test("не-сообщение — null", async () => {
  const { deps: d, gens } = deps();
  const r = await handleUpdate({ edited_message: {} }, d);
  assertEquals(r, null);
  assert(gens.length === 0);
});
```

- [ ] **Step 2: Проверить, что красный** (handler.ts отсутствует)
Run: `deno test tests/tgbot_test.ts --no-check`

- [ ] **Step 3: `_shared/cors.ts`**

```ts
export function corsHeaders(): Record<string, string> {
  return {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, x-init-data",
  };
}

export function okJson(body: unknown): Response {
  return new Response(JSON.stringify(body), {
    status: 200,
    headers: { "Content-Type": "application/json", ...corsHeaders() },
  });
}

export function errJson(status: number, message: string): Response {
  return new Response(JSON.stringify({ error: message }), {
    status,
    headers: { "Content-Type": "application/json", ...corsHeaders() },
  });
}
```

- [ ] **Step 4: `handler.ts` (чистая логика)**

```ts
import type { Db } from "../_shared/db.ts";
import { getOrCreateUser, quotaUsedToday, insertPost, logGeneration } from "../_shared/db.ts";
import { buildPostPrompt, type Rubric } from "../_shared/prompts.ts";

export interface BotDeps {
  db: Db;
  freeQuota: number;
  webappUrl: string;
  generate: (prompt: string) => Promise<{ text: string; model: string }>;
  sendMessage: (chatId: number, text: string, opts?: Record<string, unknown>) => Promise<unknown>;
}

const RUBRIC_ORDER: Rubric[] = ["польза", "вовлечение", "продажи", "личное"];

export async function handleUpdate(
  update: unknown,
  deps: BotDeps,
): Promise<"start" | "post" | "quota" | null> {
  const msg = (update as { message?: { text?: string; chat?: { id: number }; from?: { id: number; first_name?: string } } }).message;
  if (!msg?.text || !msg.chat) return null;
  const chatId = msg.chat.id;
  const text = msg.text;

  if (text === "/start") {
    await deps.sendMessage(
      chatId,
      "Привет! Я Reflectin — генерирую посты для твоего канала. Пиши тему — и я оформлю пост.",
      {
        reply_markup: {
          inline_keyboard: [[{ text: "Открыть Reflectin", web_app: { url: deps.webappUrl } }]],
        },
      },
    );
    return "start";
  }

  if (text.startsWith("/")) return null;

  if (!msg.from) return null;
  const date = new Date().toISOString().slice(0, 10);
  const dbUser = await getOrCreateUser(deps.db, msg.from.id, msg.from.first_name ?? "");
  const used = await quotaUsedToday(deps.db, dbUser.id, date);
  if (used >= deps.freeQuota) {
    await deps.sendMessage(chatId, "Лимит на сегодня исчерпан (5/5). Попробуй завтра или открой Mini App.");
    return "quota";
  }

  const rubric = RUBRIC_ORDER[text.length % RUBRIC_ORDER.length];
  const prompt = buildPostPrompt({ topic: text, rubric });
  const { text: post, model } = await deps.generate(prompt);
  await insertPost(deps.db, { user_id: dbUser.id, content: post, rubric, status: "draft" });
  await logGeneration(deps.db, { user_id: dbUser.id, feature: "post", model });
  await deps.sendMessage(chatId, post);
  return "post";
}
```

- [ ] **Step 5: `telegram-bot/index.ts` (сборка из env)**

```ts
import { handleUpdate, type BotDeps } from "./handler.ts";
import { dbFromEnv } from "../_shared/db.ts";
import { resolveModels, completeWithFallback } from "../_shared/ai.ts";

function wireDeps(): BotDeps {
  const token = Deno.env.get("BOT_TOKEN") ?? "";
  const apiKey = Deno.env.get("OPENROUTER_API_KEY") ?? "";
  const models = resolveModels(Deno.env.get("OPENROUTER_MODELS"));
  const apiBase = `https://api.telegram.org/bot${token}`;
  return {
    db: dbFromEnv(),
    freeQuota: Number(Deno.env.get("FREE_DAILY_QUOTA"\) ?? 2),
    webappUrl: Deno.env.get("WEBAPP_URL") ?? "",
    generate: (prompt) =>
      completeWithFallback({ apiKey, models, messages: [{ role: "user", content: prompt }] }),
    sendMessage: async (chatId, text, opts) => {
      const res = await fetch(`${apiBase}/sendMessage`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ chat_id: chatId, text, ...(opts ?? {}) }),
      });
      return res.json();
    },
  };
}

Deno.serve(async (req) => {
  const secret = Deno.env.get("BOT_SECRET") ?? "";
  if (req.headers.get("x-telegram-bot-api-secret-token") !== secret) {
    return new Response("forbidden", { status: 403 });
  }
  const update = await req.json().catch(() => null);
  if (update) {
    try {
      await handleUpdate(update, wireDeps());
    } catch (e) {
      console.error("webhook update failed", e);
    }
  }
  return new Response("ok", { status: 200 });
});
```

- [ ] **Step 6: Зелёный тест**
Run: `deno test tests/tgbot_test.ts`
Expected: 4 passed.

- [ ] **Step 7: Коммит**
```bash
git add supabase/functions/_shared/cors.ts supabase/functions/telegram-bot tests/tgbot_test.ts
git commit -m "feat: webhook-бот (start + генерация + квоты)"
```

---

## Task 9: Mini App API (`api` edge function)

**Files:**
- Create: `supabase/functions/api/handler.ts`, `supabase/functions/api/index.ts`
- Test: `supabase/tests/api_test.ts`

- [ ] **Step 1: Написать падающий тест**

`supabase/tests/api_test.ts`:
```ts
import { assertEquals, assert } from "jsr:@std/assert";
import { handleApiRequest, type ApiDeps } from "../functions/api/handler.ts";
import { makeFakeDb } from "./fake_db.ts";
import { signInitData, userFields } from "./fake_initdata.ts";

const BOT_TOKEN = "test-token";
const today = new Date().toISOString().slice(0, 10);

async function signedReq(path: string, init: RequestInit = {}) {
  const s = await signInitData(userFields(777, "Dima"), BOT_TOKEN);
  return new Request(`https://x/functions/v1/api${path}`, {
    ...init,
    headers: { "Content-Type": "application/json", "x-init-data": s, ...(init.headers ?? {}) },
  });
}

function deps(over: Partial<ApiDeps> = {}) {
  const base: ApiDeps = {
    botToken: BOT_TOKEN,
    db: makeFakeDb({}),
    apiKey: "k",
    models: ["m/free"],
    freeQuota: 5,
    generate: async () => ({ text: "Пост", model: "m/free" }),
    ...over,
  };
  return base as ApiDeps;
}

Deno.test("OPTIONS — CORS-заголовки", async () => {
  const res = await handleApiRequest(new Request("https://x/functions/v1/api/quota", { method: "OPTIONS" }), deps());
  assertEquals(res.status, 200);
  assertEquals(res.headers.get("Access-Control-Allow-Origin"), "*");
});

Deno.test("без initData — 401", async () => {
  const res = await handleApiRequest(new Request("https://x/functions/v1/api/quota"), deps());
  assertEquals(res.status, 401);
});

Deno.test("плохой initData — 401", async () => {
  const req = new Request("https://x/functions/v1/api/quota", {
    headers: { "x-init-data": "hash=bad&auth_date=1" },
  });
  const res = await handleApiRequest(req, deps());
  assertEquals(res.status, 401);
});

Deno.test("GET /quota — счётчик и лимит", async () => {
  const rows = [
    { user_id: "id1", created_at: `${today}T09:00:00Z` },
    { user_id: "id1", created_at: `${today}T10:00:00Z` },
  ];
  const res = await handleApiRequest(await signedReq("/quota"), deps({ db: makeFakeDb({ generation_log: rows, users: [] }) }));
  assertEquals(res.status, 200);
  const body = await res.json();
  assertEquals(body.used, 2);
  assertEquals(body.free, 5);
  assert(body.rubrics.length === 4);
});

Deno.test("POST /gen без темы — 400", async () => {
  const res = await handleApiRequest(
    await signedReq("/gen", { method: "POST", body: JSON.stringify({ rubric: "польза" }) }),
    deps(),
  );
  assertEquals(res.status, 400);
});

Deno.test("POST /gen с неверной рубрикой — 400", async () => {
  const res = await handleApiRequest(
    await signedReq("/gen", { method: "POST", body: JSON.stringify({ topic: "x", rubric: "мемы" }) }),
    deps(),
  );
  assertEquals(res.status, 400);
});

Deno.test("POST /gen при исчерпанной квоте — 429", async () => {
  const rows = [
    { user_id: "id1", created_at: `${today}T09:00:00Z` },
    { user_id: "id1", created_at: `${today}T10:00:00Z` },
    { user_id: "id1", created_at: `${today}T11:00:00Z` },
    { user_id: "id1", created_at: `${today}T12:00:00Z` },
    { user_id: "id1", created_at: `${today}T13:00:00Z` },
  ];
  const res = await handleApiRequest(
    await signedReq("/gen", { method: "POST", body: JSON.stringify({ topic: "тема", rubric: "польза" }) }),
    deps({ db: makeFakeDb({ generation_log: rows, users: [] }) }),
  );
  assertEquals(res.status, 429);
});

Deno.test("POST /gen — пост, черновик и лог", async () => {
  const db = makeFakeDb({});
  const generate = async () => ({ text: "Готовый пост", model: "m/free" });
  const res = await handleApiRequest(
    await signedReq("/gen", { method: "POST", body: JSON.stringify({ topic: "отпуск", rubric: "польза" }) }),
    deps({ db, generate }),
  );
  assertEquals(res.status, 200);
  const body = await res.json();
  assertEquals(body.post, "Готовый пост");
  assert(db.calls.some((c) => c.startsWith("insert:posts")));
  assert(db.calls.some((c) => c.startsWith("insert:generation_log")));
});

Deno.test("GET /drafts — черновики юзера", async () => {
  const db = makeFakeDb({ posts: [{ id: "p1", user_id: "id1", content: "пост", rubric: "польза", created_at: "2026-08-20T10:00:00Z", status: "draft" }] });
  const res = await handleApiRequest(await signedReq("/drafts"), deps({ db }));
  assertEquals(res.status, 200);
  const body = await res.json();
  assertEquals(body.drafts.length, 1);
});

Deno.test("неизвестный путь — 404", async () => {
  const res = await handleApiRequest(await signedReq("/nope"), deps());
  assertEquals(res.status, 404);
});
```

- [ ] **Step 2: Проверить, что красный** (`api/index.ts` отсутствует)
Run: `deno test tests/api_test.ts --no-check`

- [ ] **Step 3: Реализация `api/handler.ts`**

```ts
import { corsHeaders, okJson, errJson } from "../_shared/cors.ts";
import { validateInitData, initDataUser } from "../_shared/initdata.ts";
import { getOrCreateUser, quotaUsedToday, insertPost, logGeneration, getDrafts, type Db } from "../_shared/db.ts";
import { buildPostPrompt, RUBRICS, type Rubric } from "../_shared/prompts.ts";

export interface ApiDeps {
  botToken: string;
  db: Db;
  apiKey: string;
  models: string[];
  freeQuota: number;
  generate: (prompt: string) => Promise<{ text: string; model: string }>;
  nowSeconds?: number;
}

async function authUser(req: Request, deps: ApiDeps): Promise<{ id: number; first_name: string } | null> {
  const raw = req.headers.get("x-init-data");
  if (!raw) return null;
  const ok = await validateInitData(raw, deps.botToken, 86400, deps.nowSeconds);
  if (!ok) return null;
  return initDataUser(raw);
}

export async function handleApiRequest(req: Request, deps: ApiDeps): Promise<Response> {
  if (req.method === "OPTIONS") return new Response("ok", { headers: corsHeaders() });

  const url = new URL(req.url);
  const path = url.pathname.replace(/\/+$/, "");
  const last = path.slice(path.lastIndexOf("/") + 1);

  const user = await authUser(req, deps);
  if (!user) return errJson(401, "Невалидный initData");
  if (last === "quota" && req.method === "GET") {
    const dbUser = await getOrCreateUser(deps.db, user.id, user.first_name);
    const used = await quotaUsedToday(deps.db, dbUser.id, new Date().toISOString().slice(0, 10));
    return okJson({ used, free: deps.freeQuota, rubrics: RUBRICS, plan: dbUser.plan ?? "free" });
  }
  if (last === "drafts" && req.method === "GET") {
    const dbUser = await getOrCreateUser(deps.db, user.id, user.first_name);
    return okJson({ drafts: await getDrafts(deps.db, dbUser.id) });
  }
  if (last === "gen" && req.method === "POST") {
    const body = await req.json().catch(() => null);
    const topic = typeof body?.topic === "string" ? body.topic.trim() : "";
    const rubric = body?.rubric as Rubric;
    if (!topic) return errJson(400, "Тема обязательна");
    if (!RUBRICS.includes(rubric)) return errJson(400, "Неверная рубрика");

    const dbUser = await getOrCreateUser(deps.db, user.id, user.first_name);
    const date = new Date().toISOString().slice(0, 10);
    const used = await quotaUsedToday(deps.db, dbUser.id, date);
    if (used >= deps.freeQuota) return errJson(429, "Лимит на сегодня исчерпан");

    const prompt = buildPostPrompt({ topic, rubric });
    const { text, model } = await deps.generate(prompt);
    await insertPost(deps.db, { user_id: dbUser.id, content: text, rubric, status: "draft" });
    await logGeneration(deps.db, { user_id: dbUser.id, feature: "post", model });
    return okJson({ post: text, draft: true });
  }
  return errJson(404, "Не найдено");
}
```

- [ ] **Step 4: Реализация `api/index.ts` (обвязка)**

```ts
import { handleApiRequest, type ApiDeps } from "./handler.ts";
import { dbFromEnv } from "../_shared/db.ts";
import { resolveModels, completeWithFallback } from "../_shared/ai.ts";

Deno.serve((req) => {
  const apiKey = Deno.env.get("OPENROUTER_API_KEY") ?? "";
  const deps: ApiDeps = {
    botToken: Deno.env.get("BOT_TOKEN") ?? "",
    db: dbFromEnv(),
    apiKey,
    models: resolveModels(Deno.env.get("OPENROUTER_MODELS")),
    freeQuota: Number(Deno.env.get("FREE_DAILY_QUOTA"\) ?? 2),
    generate: (prompt) =>
      completeWithFallback({ apiKey, models: deps.models, messages: [{ role: "user", content: prompt }] }),
  };
  return handleApiRequest(req, deps);
});
```

- [ ] **Step 5: Зелёный**
Run: `deno test tests/api_test.ts`
Expected: 9 passed.

- [ ] **Step 6: Полный прогон + коммит**
Run: `deno test` — все зелёные.
```bash
git add supabase/functions/api supabase/tests/api_test.ts
git commit -m "feat: Mini App API (quota/drafts/gen, initData-auth)"
```

---

## Task 10: Скрипты деплоя и webhook

**Files:**
- Create: `scripts/deploy.ps1`, `scripts/set-webhook.ps1`, `scripts/set-menu-button.ps1`

- [ ] **Step 1: `scripts/set-webhook.ps1`**

```powershell
param(
  [Parameter(Mandatory = $true)][string]$Url,
  [Parameter(Mandatory = $true)][string]$Token,
  [string]$Secret,
  [switch]$DryRun
)
$base = "https://api.telegram.org/bot$Token"
if ($DryRun) {
  Write-Host "DRY RUN: $base/setWebhook url=$Url secret_token=$Secret allowed_updates=message,channel_post,message_reaction"
  exit 0
}
$body = @{ url = $Url; allowed_updates = @("message", "channel_post", "message_reaction") }
if ($Secret) { $body.secret_token = $Secret }
$res = Invoke-RestMethod -Method Post -Uri "$base/setWebhook" -ContentType "application/json" -Body ($body | ConvertTo-Json -Depth 3)
Write-Host "description: $($res.description)"
if (-not $res.ok) { exit 1 }
```

- [ ] **Step 2: `scripts/set-menu-button.ps1`**

```powershell
param(
  [Parameter(Mandatory = $true)][string]$Url,
  [Parameter(Mandatory = $true)][string]$Token,
  [switch]$DryRun
)
$base = "https://api.telegram.org/bot$Token"
if ($DryRun) {
  Write-Host "DRY RUN: $base/setChatMenuButton web_app=$Url"
  exit 0
}
$body = @{ menu_button = @{ type = "web_app"; text = "Открыть Reflectin"; web_app = @{ url = $Url } } }
$res = Invoke-RestMethod -Method Post -Uri "$base/setChatMenuButton" -ContentType "application/json" -Body ($body | ConvertTo-Json -Depth 4)
if (-not $res.ok) { exit 1 }
```

- [ ] **Step 3: `scripts/deploy.ps1`**

```powershell
param(
  [Parameter(Mandatory = $true)][string]$ProjectRef,
  [switch]$DryRun
)
if ($DryRun) {
  Write-Host "DRY RUN: supabase link; db push; functions deploy telegram-bot api"
  exit 0
}
supabase link --project-ref $ProjectRef; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
supabase db push;                    if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
supabase functions deploy telegram-bot api --no-verify-jwt
```

- [ ] **Step 4: Проверка dry-run**
```powershell
powershell -ExecutionPolicy Bypass -File scripts\set-webhook.ps1 -Url https://api.example.com/functions/v1/telegram-bot -Token test -Secret s -DryRun
powershell -ExecutionPolicy Bypass -File scripts\set-menu-button.ps1 -Url https://app.example.com -Token test -DryRun
powershell -ExecutionPolicy Bypass -File scripts\deploy.ps1 -ProjectRef xyz -DryRun
```
Expected: все выводят строку `DRY RUN: …` и код выхода 0.

- [ ] **Step 5: Коммит**
```bash
git add scripts
git commit -m "feat: скрипты деплоя и настройки webhook"
```

---

## Task 11: Webapp — каркас (Vite + React + TS + Tailwind v4)

**Files:**
- Create: `webapp/package.json`, `webapp/vite.config.ts`, `webapp/tsconfig.json`, `webapp/index.html`, `webapp/src/main.tsx`, `webapp/src/index.css`, `webapp/src/lib/api.ts`
- Verify: `npm run typecheck` + `npm run build`

- [ ] **Step 1: `package.json`**

```json
{
  "name": "reflectin-webapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
```

- [ ] **Step 2: `vite.config.ts`**

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

- [ ] **Step 3: `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "types": ["vite/client"]
  },
  "include": ["src"]
}
```

- [ ] **Step 4: `index.html`**

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Reflectin</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: `src/main.tsx`**

```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

const tg = (window as any).Telegram?.WebApp;
tg?.ready?.();
tg?.expand?.();

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 6: `src/index.css` (mono + accent #b4ff5a)**

```css
@import "tailwindcss";
@import "@phosphor-icons/react/dist/style.css";

@theme {
  --color-bg: #0b0b0d;
  --color-panel: #151518;
  --color-line: #222227;
  --color-ink: #f5f5f4;
  --color-sub: #a1a1a6;
  --color-dim: #6b6b73;
  --color-acc: #b4ff5a;
}

html, body, #root {
  background: #0b0b0d;
  color: #f5f5f4;
  min-height: 100%;
}
```

- [ ] **Step 7: `src/lib/api.ts`**

```ts
const env = (import.meta as any).env;
const API_BASE: string = env?.VITE_API_URL ?? "";

function initData(): string {
  return (window as any).Telegram?.WebApp?.initData ?? "";
}

async function req<T>(path: string, init?: RequestInit): Promise<T> {
  const res = await fetch(`${API_BASE}${path}`, {
    ...init,
    headers: {
      "Content-Type": "application/json",
      "x-init-data": initData(),
      ...(init?.headers ?? {}),
    },
  });
  const body = await res.json().catch(() => null);
  if (!res.ok) throw new Error(body?.error ?? `HTTP ${res.status}`);
  return body as T;
}

export interface Quota { used: number; free: number; rubrics: string[] }
export interface Draft { id: string; content: string; rubric: string; created_at: string }

export const fetchQuota = () => req<Quota>("/quota");
export const fetchDrafts = () => req<{ drafts: Draft[] }>("/drafts");
export const generatePost = (topic: string, rubric: string) =>
  req<{ post: string }>("/gen", { method: "POST", body: JSON.stringify({ topic, rubric }) });
```

- [ ] **Step 8: Установка зависимостей**
Run: `cd webapp; npm install`
Expected: lockfile создан.

- [ ] **Step 9: Проверка (App.tsx пока заглушкой)**
Временно положить `src/App.tsx` = `export default () => null;`, затем:
Run: `npm run typecheck && npm run build`
Expected: успех.

- [ ] **Step 10: Коммит**
```bash
git add webapp
git commit -m "chore: webapp каркас (Vite+React+TS+Tailwind)"
```

---

## Task 12: Webapp — стартовый экран (v4) и генератор

**Files:**
- Create: `webapp/src/App.tsx` (все экраны: Start/Generator/Drafts/Placeholder + Nav + Meter)
- Verify: `npm run typecheck` + `npm run build`; ручная проверка в браузере

- [ ] **Step 1: Написать `src/App.tsx` (полностью)**

```tsx
import { useEffect, useState, type ReactElement } from "react";
import {
  House, Plus, PenNib, DotsThree, Sparkle, Gear, Warning,
  Hazard, CalendarDots, ChartBar, Crown,
} from "@phosphor-icons/react";
import { fetchQuota, generatePost, fetchDrafts, type Quota, type Draft } from "./lib/api";

type Tab = "start" | "gen" | "drafts" | "more";

const RUBRIC_ORDER = ["польза", "вовлечение", "продажи", "личное"];
const RUBRIC_ACCENT: Record<string, string> = {
  польза: "#b4ff5a",
  вовлечение: "#8fd699",
  продажи: "#e2f17a",
  личное: "#a4b46c",
};

export default function App() {
  const [tab, setTab] = useState<Tab>("start");
  const [quota, setQuota] = useState<Quota | null>(null);

  useEffect(() => {
    fetchQuota()
      .then(setQuota)
      .catch(() => setQuota({ used: 0, free: 5, rubrics: RUBRIC_ORDER }));
  }, []);

  return (
    <div className="mx-auto flex min-h-screen w-full max-w-[400px] flex-col bg-bg text-ink">
      <main className="flex-1 px-4 pb-28 pt-4">
        {tab === "start" && <Start quota={quota} onGo={setTab} />}
        {tab === "gen" && <Generator quota={quota} onQuota={setQuota} />}
        {tab === "drafts" && <Drafts />}
        {tab === "more" && <Placeholder title="Задачи, план и аналитика — на следующем этапе" />}
      </main>
      <Nav tab={tab} onChange={setTab} />
    </div>
  );
}

function Logo() {
  return (
    <div className="mb-3 flex h-10 w-10 items-center justify-center rounded-[11px] bg-ink text-[22px] font-extrabold leading-none text-bg">
      #
    </div>
  );
}

function Meter({ used, free }: { used: number; free: number }) {
  return (
    <div className="flex items-end gap-1" title={`${used} из ${free}`}>
      {Array.from({ length: free }).map((_, i) => (
        <i key={i} className={`h-2.5 w-2 rounded-full ${i < used ? "bg-acc" : "bg-[#26262b]"}`} />
      ))}
    </div>
  );
}

function Start({ quota, onGo }: { quota: Quota | null; onGo: (t: Tab) => void }) {
  const tiles: Array<{ t: Tab; icon: ReactElement; title: string; sub: string; top: string }> = [
    { t: "drafts", icon: <PenNib size={21} />, title: "Черновики", sub: "2 готовы", top: "mt-[14px]" },
    { t: "more", icon: <Hazard size={21} className="text-[#8fd699]" />, title: "Задачи", sub: "1 в работе", top: "mt-[28px]" },
    { t: "more", icon: <CalendarDots size={21} className="text-[#e2f17a]" />, title: "План", sub: "4 поста", top: "mt-[42px]" },
    { t: "more", icon: <ChartBar size={21} className="text-[#a4b46c]" />, title: "Аналитика", sub: "за неделю", top: "mt-[14px]" },
    { t: "more", icon: <Crown size={21} />, title: "Тарифы", sub: "Free", top: "mt-[28px]" },
    { t: "more", icon: <Gear size={21} />, title: "Настройки", sub: "канал", top: "mt-[14px]" },
  ];

  return (
    <div>
      <header className="flex items-center justify-between">
        <div className="flex items-center gap-2.5">
          <div className="flex h-11 w-11 items-center justify-center rounded-[13px] bg-ink text-xl font-extrabold text-bg">#</div>
          <div>
            <div className="text-[9.5px] font-bold uppercase tracking-[0.16em] text-dim">Reflectin</div>
            <div className="text-xs text-sub">Добрый вечер</div>
          </div>
        </div>
        <Meter used={quota?.used ?? 0} free={quota?.free ?? 5} />
      </header>

      <button
        onClick={() => onGo("gen")}
        className="mt-5 flex w-full items-center justify-between rounded-2xl bg-acc p-4 text-left"
      >
        <div>
          <div className="text-[17px] font-extrabold text-bg">Генератор</div>
          <div className="text-xs text-[#3a4a1f]">
            пост из идеи · {quota ? `${quota.used} из ${quota.free} сегодня` : "…"}
          </div>
        </div>
        <Plus size={22} weight="bold" />
      </button>

      <div className="mt-4 text-[9.5px] font-bold uppercase tracking-[0.16em] text-dim">Раздел</div>

      <div className="mt-2 grid grid-cols-2 gap-2.5">
        {tiles.map((t, i) => (
          <button
            key={i}
            onClick={() => onGo(t.t)}
            className={`rounded-2xl border border-line bg-panel p-4 text-left ${t.top}`}
          >
            <span className="text-acc">{t.icon}</span>
            <div className="mt-2 text-sm font-bold">{t.title}</div>
            <div className="mt-0.5 text-[11px] text-sub">{t.sub}</div>
          </button>
        ))}
      </div>
    </div>
  );
}

function Generator({ quota, onQuota }: { quota: Quota | null; onQuota: (q: Quota) => void }) {
  const [topic, setTopic] = useState("");
  const [rubric, setRubric] = useState("польза");
  const [busy, setBusy] = useState(false);
  const [post, setPost] = useState<string | null>(null);
  const [error, setError] = useState("");

  const noQuota = quota !== null && quota.used >= quota.free;

  async function gen() {
    if (!topic.trim() || busy || noQuota) return;
    setBusy(true);
    setError("");
    setPost(null);
    try {
      const r = await generatePost(topic.trim(), rubric);
      setPost(r.post);
      const q = await fetchQuota();
      onQuota(q);
    } catch (e) {
      setError(String((e as Error)?.message ?? e));
    } finally {
      setBusy(false);
    }
  }

  return (
    <div>
      <Logo />
      <div className="text-[9.5px] font-bold uppercase tracking-[0.16em] text-dim">Новый пост</div>
      <h1 className="mt-1 text-lg font-extrabold">О чём напишем?</h1>

      <textarea
        value={topic}
        onChange={(e) => setTopic(e.target.value)}
        placeholder="Например: как не выгореть в найме…"
        rows={2}
        className="mt-3 w-full resize-none rounded-xl border border-line bg-panel p-3 text-[13px] text-ink placeholder:text-dim focus:border-acc focus:outline-none"
      />

      <div className="mt-3 flex flex-wrap gap-1.5">
        {RUBRIC_ORDER.map((r) => (
          <button
            key={r}
            onClick={() => setRubric(r)}
            className={
              r === rubric
                ? "rounded-full bg-acc px-3 py-1.5 text-[11px] font-bold text-bg"
                : "rounded-full border border-line bg-panel px-3 py-1.5 text-[11px] font-bold text-ink"
            }
          >
            {r}
          </button>
        ))}
      </div>

      {post && (
        <div
          className="mt-4 rounded-2xl border border-line bg-panel p-4"
          style={{ borderLeft: `4px solid ${RUBRIC_ACCENT[rubric] ?? "#b4ff5a"}` }}
        >
          <div className="text-[11px] text-sub">Превью поста</div>
          <div className="mt-2 whitespace-pre-wrap text-[13px] leading-relaxed">{post}</div>
        </div>
      )}

      {error && (
        <div className="mt-3 flex items-center gap-2 text-xs text-[#ff8f8f]">
          <Warning size={16} />
          {error}
        </div>
      )}

      <button
        onClick={gen}
        disabled={busy || noQuota}
        className="mt-4 flex w-full items-center justify-center gap-2 rounded-[13px] bg-acc py-3.5 text-[14px] font-bold text-bg disabled:opacity-40"
      >
        <Sparkle size={18} weight="bold" />
        {busy ? "Генерирую…" : noQuota ? "Лимит на сегодня исчерпан" : "Сгенерировать"}
      </button>
      {noQuota && (
        <p className="mt-2 text-center text-[11px] text-sub">
          Лимит Free — 5 постов в день. Черновики и план без ограничений.
        </p>
      )}
    </div>
  );
}

function Drafts() {
  const [drafts, setDrafts] = useState<Draft[]>([]);
  const [error, setError] = useState("");

  useEffect(() => {
    fetchDrafts()
      .then((r) => setDrafts(r.drafts))
      .catch((e) => setError(String((e as Error)?.message ?? e)));
  }, []);

  return (
    <div>
      <Logo />
      <div className="text-[9.5px] font-bold uppercase tracking-[0.16em] text-dim">Редактор</div>
      <h1 className="mt-1 text-lg font-extrabold">Черновики</h1>

      {error && <p className="mt-3 text-xs text-[#ff8f8f]">{error}</p>}
      {drafts.map((d) => (
        <div
          key={d.id}
          className="mt-3 rounded-2xl border border-line bg-panel p-4"
          style={{ borderLeft: `4px solid ${RUBRIC_ACCENT[d.rubric] ?? "#b4ff5a"}` }}
        >
          <div className="text-[11px] text-sub">{d.rubric}</div>
          <div className="mt-1 whitespace-pre-wrap text-[13px] leading-relaxed">
            {d.content.slice(0, 220)}
            {d.content.length > 220 ? "…" : ""}
          </div>
        </div>
      ))}
      {!error && drafts.length === 0 && (
        <p className="mt-8 text-center text-sm text-sub">
          Черновиков пока нет — сгенерируй первый.
        </p>
      )}
    </div>
  );
}

function Placeholder({ title }: { title: string }) {
  return <p className="pt-10 text-center text-sm text-sub">{title}</p>;
}

function Nav({ tab, onChange }: { tab: Tab; onChange: (t: Tab) => void }) {
  const items: Array<{ key: Tab; icon: typeof House; label: string }> = [
    { key: "start", icon: House, label: "Дом" },
    { key: "gen", icon: Plus, label: "Создать" },
    { key: "drafts", icon: PenNib, label: "Черновики" },
    { key: "more", icon: DotsThree, label: "Ещё" },
  ];
  return (
    <div className="fixed bottom-0 left-1/2 w-full max-w-[400px] -translate-x-1/2 border-t border-line bg-bg/95 px-4 pb-2 pt-1.5 backdrop-blur">
      <div className="flex gap-1">
        {items.map(({ key, icon: Icon, label }) => (
          <button
            key={key}
            onClick={() => onChange(key)}
            className={`flex flex-1 flex-col items-center gap-1 py-1 text-[10px] ${tab === key ? "text-acc" : "text-dim"}`}
          >
            <Icon size={20} weight={tab === key ? "fill" : "regular"} />
            <span>{label}</span>
          </button>
        ))}
      </div>
    </div>
  );
}
```

> Примечание: в экране Start карточка «Черновики» показывает пустой `sub` при наличии квоты — это намеренно (подсказка-счётчик в Генераторе). Не спорить с дизайном в треде.

- [ ] **Step 2: Typecheck + build**
Run: `cd webapp; npm run typecheck; npm run build`
Expected: без ошибок, `dist/` появился.

- [ ] **Step 3: Ручная проверка (браузер)**
- `npm run dev`, адрес `http://localhost:5173`.
- Стартовый экран: фон монохромный, логотип `#`, лесенка табов, сегменты квоты, нижняя навигация.
- Вкладка «Создать»: чипы рубрик, кнопка «Сгенерировать», состояние busy.

- [ ] **Step 4: Коммит**
```bash
git add webapp/src
git commit -m "feat: стартовый экран v4 и генератор постов"
```

---

## Task 13: Webapp — черновики + интеграция с API + секреты

**Files:**
- Modify: `webapp/src/App.tsx` (Drafts уже включён выше; здесь — финальная сверка + .env)
- Create: `webapp/.env.example`

- [ ] **Step 1: `webapp/.env.example`**

```dotenv
VITE_API_URL=https://<project-ref>.supabase.co/functions/v1/api
```

- [ ] **Step 2: Проверка в браузере на реальном api (если задеплоено) или фейк-мок**

Если api ещё не задеплоен — поднять локальный фейк:
```powershell
# подтверждение: без переменной /quota вернёт 401 с сообщением "Невалидный initData"
invoke-webrequest -Uri "https://<project-ref>.supabase.co/functions/v1/api/quota" -OutVariable r
```
Ожидание: `{"error":"Невалидный initData"}`.

- [ ] **Step 3: Финальный `deno test` + build**
Run: `powerShell -c "cd D:\Bots\reflectin\supabase; deno test"`
Run: `powershell -c "cd D:\Bots\reflectin\webapp; npm run typecheck; npm run build"`
Expected: зелёное.

- [ ] **Step 4: Коммит**
```bash
git add webapp/.env.example webapp/src
git commit -m "feat: черновики + проверка интеграции с api"
```

---

## Сводная проверка (после всех задач)

```powershell
cd D:\Bots\reflectin\supabase; deno test      # все зелёные
cd D:\Bots\reflectin\webapp; npm run build    # успех
powershell -File scripts\set-webhook.ps1 -Url <fn-url> -Token $env:BOT_TOKEN -Secret $env:BOT_SECRET -DryRun
powershell -File scripts\set-menu-button.ps1 -Url <webapp-url> -Token $env:BOT_TOKEN -DryRun
```

## Self-Review

- **Спека покрыта:** поведение bota (start/webapp/generated) → Task 8; Mini App API + квоты + черновики → Task 9, 12, 13; initData auth → Task 3; ростер моделей без хардкода → Task 5; схема → Task 6; деплой/скрипты → Task 10. Дизайн v4 (mono, `#`, лесенка, #b4ff5a, сегменты) → Task 12.
- **Placeholders:** нет «чет добавить логику» — весь код приведён. В api `generate` — динамический импорт ai-модуля, чтобы тесты api не тянули сеть.
- **Типы согласованы:** `generate(prompt) => {text, model}` единый у бота и api; `BotDeps`/`ApiDeps` параметры совпадают с `_shared`-контрактами; `RUBRIC_ORDER` и `RUBRIC_ACCENT` на фронте соответствуют `RUBRICS`/весам.